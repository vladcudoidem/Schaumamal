package model

import model.platform.PlatformInformationProvider

class Dumper(
    platformInformationProvider: PlatformInformationProvider
) {
    /*
    * Todo:
    *  - Look into:
    *     - Adb utils.
    *     - How to take screenshots of all screens.
    *  - Place the dumped data in the tmp directory and create Dump data objects.
    * */
}